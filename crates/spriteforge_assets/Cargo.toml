[package]
name = "spriteforge_assets"
version = "0.1.0"
edition = "2024"

description = "Shared asset metadata and mask utilities for Spriteforge"
license = "MIT"

[dependencies]
serde = { version = "1.0.210", features = ["derive"] }
serde_json = "1.0.128"
